use anyhow::Result;

#[tokio::main]
async fn main() -> Result<()> {
    // Qui andrà la logica per inizializzare tracing, leggere la config,
    // parsare gli argomenti della CLI e avviare l'applicazione.
    println!("Benvenuto in Ceres!");

    Ok(())
}
